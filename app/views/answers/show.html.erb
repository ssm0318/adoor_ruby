<%= render partial: "./user_profile", locals: {user: @answer.author} %><br/>
<% if current_user.id == @answer.author.id %>
    <%= link_to "수정하기", edit_answer_path(@answer.id) %>
    <%= link_to "삭제하기", answer_path(@answer.id),
                            method: :delete,
                            data: { confirm: '답변을 지우시겠습니까?'} %> 
    <%# TODO: 공개범위 수정하는거 구현 %>
    <br/>
<% end %>
<%= @answer.created_at %><br/>
Q. <%= @answer.question.content %> 
<% if Star.where(user_id: current_user.id, target: @answer.question).empty? %>
    <%= link_to "별표하기", stars_path(target_id: @answer.question.id, target_type: "Question"), method: :post %>
<% else %>
    <%= link_to "별표 취소하기", star_path(@answer.question.id, target_type: "Question"), method: :delete %>
<% end %>
<br/>
A. <%= @answer.content %><br/>
<%# 여기도. 이런 코드를 view에서 쓰는 게 맞는지 %>
<% if Star.where(user_id: current_user.id, target: @answer).empty? %>
    <%= link_to "별표하기", stars_path(target_id: @answer.id, target_type: "Answer"), method: :post %>
<% else %>
    <%= link_to "별표 취소하기", star_path(@answer.id, target_type: "Answer"), method: :delete %>
<% end %>
