<% if Like.where(user_id: current_user.id, target: a).empty? %>
    <%= link_to image_tag("icons/like-default-icon", class: "btn-like"), likes_path(target_id: a.id, target_type: target_type), method: :post %>
<% else %>
    <%= link_to image_tag("icons/like-on-icon", class: "btn-like"), like_path(a.id, target_type: target_type), method: :delete, data: {confirm: '좋아요를 취소하겠습니까?'} %>
<% end %>