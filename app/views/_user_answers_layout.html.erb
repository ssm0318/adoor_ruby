<%= render partial: "./user_profile", locals: {user: user} %>님의 페이지 <br/>
<%#= if current_user.id == user.id 가 들어가야 할 곳들은 "TODO: 본인확인", 
if current_user.id != user.id 가 들어가야 할 곳들은 "TODO: 타인확인" 으로 표시해뒀음 %>

<%#= TODO: 본인확인 %>
    <%#=<%= link_to "내 정보 수정하기", edit_user_profile_path(@user) %> 
<%= link_to "답변", user_answers_path(user.id) %>
<%#= TODO: 본인확인%>
    <%= link_to "형광펜", user_highlights_path(user.id) %>
    <%= link_to "별", user_stars_path(user.id) %>
