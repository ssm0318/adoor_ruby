<%= link_to image_tag(user.image.url, :style => "height:20px; width:20px; border-radius:10px; margin-right: 2px;"), profile_path(user.slug) %>
<%= link_to user.username, profile_path(user.slug), class: "username", :style => "font-size: 14px;"%>