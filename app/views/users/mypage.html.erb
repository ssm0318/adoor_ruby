<h1>내 정보 수정하기</h1>

<div>
    <p>프로필 사진
        <%= image_tag current_user.image.url, :style => "height:40px; width:40px; border-radius:15px;" %>
    </p>
    <p>이메일 <%= current_user.email %></p>
    <p>사용자명
        <% if current_user.username.nil? %>-
        <% else %>
            <%= current_user.username %>
        <% end %>
    </p>
    <p>생년월일
        <% if current_user.date_of_birth.nil? %>-
        <% else %>
            <%= current_user.date_of_birth %>
        <% end %>
    </p>
</div>

<%= link_to "내 정보 수정하기", edit_user_profile_url(current_user.id) %>
<%= link_to "비밀번호 변경", edit_user_registration_url(current_user.id) %>