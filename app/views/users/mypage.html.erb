<div class="sign-box">
<h1 class="sign-title">내 정보</h1>

<div class="sign-content-box" align="center">
    <%= image_tag current_user.image.url, :style => "height:50px; width:50px; border-radius:15px;" %>
    <p><strong>이메일</strong> <%= current_user.email %></p>
    <p><strong>사용자명</strong>
        <% if current_user.username.nil? %>-
        <% else %>
            <%= current_user.username %>
        <% end %>
    </p>
    <p><strong>생년월일</strong>
        <% if current_user.date_of_birth.nil? %>-
        <% else %>
            <%= current_user.date_of_birth %>
        <% end %>
    </p>
</div>
<br/>
<div class="mypage-buttons" align="center">
    <%= link_to "내 정보 수정", edit_user_profile_url(current_user.id), class: "profile-button" %>
    <%= link_to "비밀번호 변경", edit_user_registration_path(current_user.id), class: "profile-button" %>
</div>