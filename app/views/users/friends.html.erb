<div class="prism-title"><h1>친구 목록</h1></div>
<div class="friend-list">
    
    <%= render partial: "channels/new" %>
    <!-- 전체 친구 -->
    전체<br>
    <%= render partial: "friend_list", locals: {friends: @friends} %>

    <!-- 채널별 친구 -->
    <div> <!-- FIXME: 리스트 디자인이.. 엉망스가 된다 ㅠㅠ -->
        <% @channels.each do |channel| %>
            <%= channel.name %><br/>
            <%= render partial: "friend_list", locals: {friends: channel.friends} %><br>
        <% end %>
    </div>
    <% if @friends.empty? %>
        친구를 초대해보세요!<br> <!-- FIXME: 버그인가 왜 줄바꿈이 안되지 -->
        <%= link_to "친구 초대하기", invitation_path %>
    <% end %>
</div>
